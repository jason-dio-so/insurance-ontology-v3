# Phase 5 QA Evaluation 분석 결과

## 📊 전체 결과

```
Overall Accuracy: 60.0% (30/50)
Target:           90.0% ❌ FAIL
Errors:           5 queries
Latency (P95):    5,449ms (목표: <5,000ms) ❌
```

---

## 🔍 상세 분석

### 1. 카테고리별 성공률

| Category | Accuracy | Details |
|----------|----------|---------|
| ✅ **Premium** | 100.0% (2/2) | 보험료 쿼리 완벽 |
| ✅ **Condition** | 100.0% (4/4) | 가입조건 완벽 |
| ✅ **Basic** | 90.0% (9/10) | 기본 쿼리 우수 |
| ✅ **Comparison** | 83.3% (5/6) | 비교 쿼리 양호 |
| ⚠️ **Gender** | 66.7% (4/6) | 성별 쿼리 개선 필요 |
| ❌ **Amount** | 33.3% (4/12) | **치명적 문제** |
| ❌ **Age** | 25.0% (1/4) | **치명적 문제** |
| ❌ **Edge Case** | 16.7% (1/6) | 예외 케이스 실패 |

### 2. 실패 패턴 분석

#### A. Amount 카테고리 (33.3% 성공률)

**실패한 쿼리 예시**:
- Q001: "삼성화재 암 진단금" ❌
- Q002: "DB손보 뇌출혈" ❌
- Q005: "메리츠 암진단" ❌

**성공한 쿼리 예시**:
- Q003: "롯데 급성심근경색 **500만원**" ✅
- Q004: "한화 수술비 **100만원**" ✅
- Q011: "한화 골절진단 **10만원**" ✅

**핵심 차이점**:
```
성공 쿼리: 쿼리 자체에 금액 포함 ("500만원")
실패 쿼리: 금액 없이 "얼마인가?" 묻는 형태
```

**근본 원인**:
1. Vector search는 `document_clause.clause_text`만 검색
2. Context assembly는 clause 텍스트 + 문서 메타데이터만 LLM에 전달
3. **`benefit.benefit_amount` 같은 구조화된 데이터는 LLM에 전달되지 않음**
4. LLM이 clause 텍스트에서 금액을 찾을 수 없어 "정보 없음" 응답

**데이터 확인**:
```sql
-- Benefit 테이블에 금액 데이터는 존재함
SELECT coverage_name, benefit_amount FROM benefit
WHERE coverage_name LIKE '%암%진단%' LIMIT 5;

-- 결과:
일반암진단비Ⅱ      | 30,000,000
재진단암진단비      | 10,000,000
고액치료비암진단비  | 10,000,000
```

**문제**: 이 금액 정보가 LLM context에 포함되지 않음

#### B. Age 카테고리 (25.0% 성공률)

**실패한 쿼리**:
- Q019: "DB 40세 이하 가입 가능 상품" ❌
- Q020: "DB 41세 이상 암보장" ❌
- Q021: "DB 40세 이하 뇌출혈" ❌

**근본 원인**:
1. DB 보험사는 연령별로 문서를 분리 (40세 이하 / 41세 이상)
2. 이 정보가 `document.attributes` JSON에 저장되어 있음:
   ```json
   {"target_age_range": "≤40"}
   ```
3. Vector search는 이 메타데이터를 필터링 조건으로 사용하지 않음
4. LLM에도 연령 필터 정보가 명시적으로 전달되지 않음

#### C. Gender 카테고리 (66.7% 성공률)

**실패한 쿼리**:
- Q013: "롯데 여성용 암 진단 보장" ❌
- Q017: "롯데 여성 골절진단비" ❌

**근본 원인**:
1. 롯데는 성별로 문서 분리 (남성 / 여성)
2. `document.attributes`에 `{"target_gender": "female"}` 저장
3. Vector search가 성별 메타데이터를 활용하지 못함

#### D. Transaction 에러 (5건)

**에러 발생 순서**:
```
Q046: "1억원 암 진단금"
→ ERROR: invalid input syntax for type integer: ""
→ PostgreSQL transaction ABORT

Q047-Q050: 모두 "current transaction is aborted" 에러
```

**근본 원인**:
1. Q046 처리 중 integer 파싱 에러 발생
2. 아마도 "1억원"을 파싱하려다 실패 (한국어 금액 파싱 이슈)
3. Transaction이 abort되고 rollback 없이 다음 쿼리 진행
4. Abort된 transaction에서는 모든 쿼리 실패

**파일 위치**:
- `retrieval/hybrid_retriever.py` - transaction 관리 개선 필요

---

## 🎯 핵심 문제 요약

### 문제 1: 구조화 데이터 누락 (가장 심각)

**현재 아키텍처**:
```
Vector Search → clause_text 검색
Context Assembly → clause_text + 문서 메타데이터만 전달
LLM → clause_text에서 답변 생성
```

**누락된 정보**:
- `benefit.benefit_amount` (보장금액)
- `benefit.benefit_type` (보장 유형)
- `coverage.coverage_metadata` (커버리지 상세)
- `document.attributes` (연령, 성별 필터)

**영향**:
- Amount 쿼리 33.3% 성공률
- Age 쿼리 25.0% 성공률
- Gender 쿼리 66.7% 성공률

### 문제 2: 메타데이터 필터링 부재

**현재**:
- Vector search는 전체 clause에서 유사도만 계산
- 연령, 성별, 보장금액 같은 메타데이터 필터 없음

**필요**:
- "40세 이하" 쿼리 → `document.attributes.target_age_range` 필터
- "여성용" 쿼리 → `document.attributes.target_gender` 필터
- "3천만원" 쿼리 → `benefit.benefit_amount` 범위 필터

### 문제 3: Transaction 관리 부실

**현상**:
- Q046에서 파싱 에러 발생
- Transaction abort 후 복구 없이 진행
- 이후 5개 쿼리 모두 실패

**필요**:
- Try-catch로 각 쿼리 독립적으로 처리
- Transaction rollback 후 새 transaction 시작

---

## 💡 해결 방안

### 방안 1: Context Enrichment (즉시 개선 가능) ⭐️ 추천

**목적**: LLM context에 구조화된 데이터 포함

**구현**:
1. `retrieval/context_assembly.py`의 `_enrich_with_metadata()` 수정
2. Clause와 연결된 coverage/benefit 정보 조회
3. LLM context에 추가:
   ```
   담보명: 일반암진단비Ⅱ
   보장금액: 30,000,000원 (3천만원)
   보장유형: 진단
   ```

**예상 효과**:
- Amount 카테고리: 33% → 80%+
- Overall accuracy: 60% → 75-80%

**구현 위치**:
```python
# retrieval/context_assembly.py
def _enrich_with_metadata(self, results):
    # 기존 코드 유지

    # 추가: Coverage/Benefit 정보 조회
    cur.execute("""
        SELECT
            dc.id as clause_id,
            c.coverage_name,
            b.benefit_amount,
            b.benefit_type
        FROM document_clause dc
        LEFT JOIN clause_coverage cc ON dc.id = cc.clause_id
        LEFT JOIN coverage c ON cc.coverage_id = c.id
        LEFT JOIN benefit b ON c.id = b.coverage_id
        WHERE dc.id = ANY(%s)
    """, (clause_ids,))

    # Context 텍스트에 추가
```

**소요 시간**: 1-2시간

---

### 방안 2: Metadata Filtering (중기 개선)

**목적**: Vector search에 메타데이터 필터 추가

**구현**:
1. `retrieval/hybrid_retriever.py` 수정
2. 쿼리에서 연령, 성별, 금액 추출
3. PostgreSQL WHERE 조건에 추가:
   ```sql
   WHERE embedding <=> query_embedding < threshold
     AND document.attributes->>'target_age_range' LIKE '%40세%'
     AND benefit.benefit_amount BETWEEN 20000000 AND 40000000
   ```

**예상 효과**:
- Age 카테고리: 25% → 75%+
- Gender 카테고리: 67% → 90%+
- Overall accuracy: 75% → 85%+

**소요 시간**: 3-4시간

---

### 방안 3: Transaction Isolation (즉시 개선 가능)

**목적**: 쿼리별 독립적 transaction 처리

**구현**:
```python
# scripts/evaluate_qa.py 또는 api/cli.py
def evaluate_query(self, query_obj):
    try:
        result = self.cli.hybrid_query(query_text)
    except Exception as e:
        # Rollback 후 connection 재생성
        self.cli.pg_conn.rollback()
        # 에러 기록하고 다음 쿼리 진행
        return {"status": "error", "error": str(e)}
```

**예상 효과**:
- Error 5건 → 1건
- Overall accuracy: 60% → 68% (에러 케이스 중 4개는 정상 처리 가능)

**소요 시간**: 30분

---

## 📋 권장 실행 순서

### 즉시 실행 (1-2시간)
1. ✅ **방안 3**: Transaction 격리 (30분)
2. ✅ **방안 1**: Context enrichment (1-2시간)
3. ✅ **Phase 5 재실행**: 정확도 측정

**예상 결과**: 60% → 75-80%

### 추가 개선 (필요 시)
4. ⏳ **방안 2**: Metadata filtering (3-4시간)
5. ⏳ **Phase 5 재실행**: 정확도 측정

**예상 결과**: 75-80% → 85-90%

---

## 🔬 추가 분석 필요 사항

### 1. 성공한 쿼리 분석
- Comparison (83%) 성공 이유 분석
- Condition (100%) 완벽 성공 이유 분석

### 2. Latency 개선
- P95: 5,449ms (목표: <5,000ms)
- Vector search 최적화 (인덱스, 배치)
- LLM 호출 최적화 (streaming, caching)

### 3. Edge Case 개선
- "보험 없는 회사", "모든 보험사" 등
- 범위 쿼리 처리 개선

---

## 📊 현재 vs 목표

| Metric | 현재 | 방안 1+3 | 방안 1+2+3 | 목표 |
|--------|------|----------|------------|------|
| Overall | 60% | **75-80%** | **85-90%** | 90% |
| Amount | 33% | **80%+** | **85%+** | 90% |
| Age | 25% | 50% | **75%+** | 85% |
| Gender | 67% | 75% | **90%+** | 90% |
| Errors | 5 | **1** | **0-1** | 0 |

---

---

## ✅ Phase 5 v2 - 개선 결과 (2025-12-11 09:00 KST)

### 구현한 개선 사항

**방안 1: Context Enrichment** (`retrieval/context_assembly.py`)
- Coverage/Benefit 정보를 LLM context에 추가
- SQL 쿼리로 clause_id → coverage → benefit 데이터 조회
- 한국어 금액 포맷팅 (예: "3천만원 (30,000,000원)")
- 보장유형 한글 변환 (diagnosis → 진단, surgery → 수술 등)

**방안 3: Transaction Isolation** (`scripts/evaluate_qa.py`)
- 각 쿼리별 독립적 transaction 처리
- Exception handler에 rollback 로직 추가
- 에러 발생 시 다음 쿼리에 영향 없도록 격리

### 개선 효과

| Metric | v1 (Before) | v2 (After) | 변화 | 상태 |
|--------|-------------|------------|------|------|
| **Overall Accuracy** | 60.0% (30/50) | **72.0% (36/50)** | **+12%** | ✅ 개선 |
| **Errors** | 5 queries | **0 queries** | **-100%** | ✅ 해결 |
| **Amount** | 33.3% (4/12) | **41.67% (5/12)** | +8.3% | ⚠️ 미흡 |
| **Age** | 25.0% (1/4) | **75.0% (3/4)** | **+50%** | ✅ 대폭 개선 |
| **Gender** | 66.7% (4/6) | **83.33% (5/6)** | +16.7% | ✅ 개선 |
| **Edge Case** | 16.7% (1/6) | **83.33% (5/6)** | **+66.7%** | ✅ 대폭 개선 |
| **Basic** | 90.0% (9/10) | **80.0% (8/10)** | -10% | ⚠️ 소폭 하락 |
| **Latency P95** | 5,449ms | 6,158ms | +709ms | ⚠️ 증가 |

### 주요 성과

✅ **Transaction 에러 완전 해결**: 5건 → 0건
✅ **Age 카테고리 극적 개선**: 25% → 75% (+50%)
✅ **Edge Case 극적 개선**: 16.7% → 83.3% (+66.7%)
✅ **Overall 12% 향상**: 60% → 72%

### 남은 문제

❌ **Amount 카테고리 여전히 낮음**: 41.67% (목표: 90%)
- 5/12만 성공, 7개 쿼리 여전히 실패
- 일부 coverage에 benefit_amount 데이터 없을 가능성
- 추가 분석 필요

⚠️ **Latency 증가**: P95 5,449ms → 6,158ms (+709ms)
- Coverage/Benefit 조회 쿼리 추가로 인한 증가
- 여전히 목표 <5,000ms 초과
- 쿼리 최적화 필요 (인덱스, 배치 처리)

⚠️ **Basic 카테고리 소폭 하락**: 90% → 80%
- 1개 쿼리 추가 실패 (9/10 → 8/10)
- Context enrichment 부작용 가능성 (noise 증가?)
- 원인 분석 필요

### 다음 단계 제안

#### 우선순위 1: Amount 카테고리 분석 (2시간)
1. 실패한 7개 쿼리 상세 분석
2. benefit_amount 데이터 누락 확인
3. 누락 시: 데이터 보강 또는 fallback 로직

#### 우선순위 2: Latency 최적화 (2-3시간)
1. Coverage/Benefit 조회 쿼리에 인덱스 추가
2. 배치 조회 최적화
3. 불필요한 JOIN 제거

#### 우선순위 3 (선택적): 방안 2 구현 (3-4시간)
- Metadata filtering (age, gender, amount 필터)
- 예상 효과: Overall 72% → 85%+

---

**마지막 업데이트**: 2025-12-11 09:00 KST
**현재 상태**: Overall 72.0%, Errors 0
**목표**: 90% (Gap: -18%)
**다음 단계**: Amount 카테고리 분석 + Latency 최적화
