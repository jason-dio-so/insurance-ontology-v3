# 파싱 파이프라인 가이드

**생성일**: 2025-12-14
**목적**: 데이터 흐름과 작업 순서 이해

---

## 1. 전체 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           원본 문서 (PDF)                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  📄 terms (약관) 9건        📄 proposal (가입설계서) 10건                    │
│     129,667 articles           690 table_rows                               │
└───────────┬─────────────────────────────────┬───────────────────────────────┘
            │                                 │
            ▼                                 ▼
┌───────────────────────────┐   ┌───────────────────────────────────────────┐
│      ingest_v3.py         │   │      proposal_plan_extractor.py           │
│  (기존 파싱 파이프라인)    │   │  (신규 - Plan 메타데이터 추출)            │
└───────────┬───────────────┘   └─────────────────┬─────────────────────────┘
            │                                     │
            ▼                                     ▼
┌───────────────────────────┐   ┌───────────────────────────────────────────┐
│     document_clause       │   │              plan (10건)                  │
│     (129,667건)           │   │  - target_gender, target_age              │
│  - clause_text            │   │  - insurance_period, payment_period       │
│  - structured_data        │   │  - total_premium                          │
└───────────┬───────────────┘   └─────────────────┬─────────────────────────┘
            │                                     │
            │                                     ▼
            │                   ┌───────────────────────────────────────────┐
            │                   │        plan_coverage (1,086건)            │
            │                   │  - plan_id → coverage_id                  │
            │                   │  - sum_insured, premium                   │
            │                   └───────────────────────────────────────────┘
            │
            ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                         clause_coverage (4,903건)                          │
│                    문서 조항 ↔ 담보 연결 테이블                             │
└───────────┬───────────────────────────┬───────────────────────────────────┘
            │                           │
            ▼                           ▼
┌─────────────────────┐   ┌─────────────────────┐   ┌─────────────────────┐
│ risk_event_extractor│   │ exclusion_extractor │   │condition_extractor  │
│  "정의" 패턴 매칭   │   │  "지급않음" 패턴    │   │ "보장개시" 패턴     │
└──────────┬──────────┘   └──────────┬──────────┘   └──────────┬──────────┘
           │                         │                         │
           ▼                         ▼                         ▼
┌─────────────────────┐   ┌─────────────────────┐   ┌─────────────────────┐
│   risk_event        │   │     exclusion       │   │     condition       │
│     (572건)         │   │      (28건)         │   │      (42건)         │
│  - event_type       │   │  - exclusion_type   │   │  - condition_type   │
│  - icd_code_pattern │   │  - is_absolute      │   │  - waiting_period   │
└─────────────────────┘   └─────────────────────┘   └─────────────────────┘
            │                         │                         │
            └─────────────────────────┼─────────────────────────┘
                                      │
                                      ▼
                        ┌─────────────────────────┐
                        │     graph_loader.py     │
                        │   PostgreSQL → Neo4j    │
                        └─────────────────────────┘
                                      │
                                      ▼
                        ┌─────────────────────────┐
                        │        Neo4j            │
                        │  그래프 데이터베이스    │
                        └─────────────────────────┘
```

---

## 2. 테이블 간 관계도

```
                                 ┌──────────────┐
                                 │   Company    │
                                 └──────┬───────┘
                                        │ 1:N
                                 ┌──────▼───────┐
                                 │   Product    │
                                 └──────┬───────┘
                                        │ 1:N
              ┌─────────────────────────┼─────────────────────────┐
              │                         │                         │
       ┌──────▼───────┐          ┌──────▼───────┐          ┌──────▼───────┐
       │   Document   │          │    Plan      │          │   Coverage   │
       │  (terms 등)  │          │ (가입설계)   │          │   (담보)     │
       └──────┬───────┘          └──────┬───────┘          └──────┬───────┘
              │                         │                         │
              │                         │ N:M                     │ 1:N
              │                  ┌──────▼───────┐          ┌──────┴───────┐
              │                  │plan_coverage │          │              │
              │                  └──────────────┘    ┌─────▼────┐  ┌──────▼─────┐
              │                                      │Condition │  │ Exclusion  │
       ┌──────▼───────┐                              │ (42건)   │  │  (28건)    │
       │document_clause│                             └──────────┘  └────────────┘
       │ (129,667건)  │
       └──────┬───────┘
              │ N:M
       ┌──────▼───────┐         ┌──────────────┐
       │clause_coverage│◄───────│   Benefit    │
       │  (4,903건)   │         └──────┬───────┘
       └──────────────┘                │ N:M
                                ┌──────▼───────┐
                                │benefit_risk  │
                                │   _event     │
                                └──────┬───────┘
                                       │
                                ┌──────▼───────┐
                                │  RiskEvent   │
                                │   (572건)    │
                                └──────────────┘
```

---

## 3. 작업 순서 및 의존성

### Phase 1: 기초 데이터 (기존 완료)
```
순서 1: Company, Product 등록
순서 2: Document 등록 (terms, proposal PDF)
순서 3: ingest_v3.py 실행 → document_clause 생성
순서 4: Coverage 등록 및 clause_coverage 연결
```

### Phase 2: 확장 데이터 추출 (이번 작업)

| 순서 | 작업 | 의존성 | 스크립트 |
|------|------|--------|----------|
| **1** | Plan 추출 | Document (proposal) | `proposal_plan_extractor.py` |
| **2** | plan_coverage 연결 | Plan + Coverage | (위 스크립트에 포함) |
| **3** | RiskEvent 추출 | document_clause (terms) | `risk_event_extractor.py` |
| **4** | Exclusion 추출 | clause_coverage 연결 필요 | `exclusion_extractor.py` |
| **5** | Condition 추출 | clause_coverage 연결 필요 | `condition_extractor.py` |

### Phase 3: Neo4j 동기화

| 순서 | 작업 | 스크립트 |
|------|------|----------|
| 1 | Plan → Neo4j | `graph_loader.py --sync-plans` |
| 2 | RiskEvent → Neo4j | `graph_loader.py --sync-risk-events` |
| 3 | Exclusion → Neo4j | `graph_loader.py --sync-exclusions` |
| 4 | Condition → Neo4j | `graph_loader.py --sync-conditions` |

---

## 4. 핵심 연결 포인트

### 4.1 clause_coverage 테이블의 역할

```sql
-- clause_coverage: 문서 조항과 담보를 연결하는 핵심 테이블
SELECT * FROM clause_coverage LIMIT 3;

-- clause_id  | coverage_id
-- -----------+------------
--      33284 |        406   (뇌혈관질환 관련 조항)
--      87445 |        406
--     111369 |        408   (뇌출혈 관련 조항)
```

**이 테이블이 있어야:**
- Exclusion 추출 시 어느 담보의 면책인지 연결 가능
- Condition 추출 시 어느 담보의 보장조건인지 연결 가능

### 4.2 Coverage가 중심인 이유

```
                    Coverage (담보)
                         │
         ┌───────────────┼───────────────┐
         │               │               │
    Condition       Exclusion       Benefit
   (보장조건)       (면책사유)      (급부금)
         │                               │
         │                          RiskEvent
    90일 대기                       (위험이벤트)
```

- **Coverage**는 보험상품의 기본 단위
- Plan은 여러 Coverage를 조합한 패키지
- Condition/Exclusion/Benefit은 Coverage에 종속

---

## 5. 실행 명령어 정리

### 전체 추출 실행 (순서대로)
```bash
# 1. Plan 추출 (proposal 문서 필요)
python -m ingestion.proposal_plan_extractor

# 2. RiskEvent 추출 (terms 문서 필요)
python -m ingestion.risk_event_extractor

# 3. Exclusion 추출 (clause_coverage 연결 필요)
python -m ingestion.exclusion_extractor

# 4. Condition 추출 (clause_coverage 연결 필요)
python -m ingestion.condition_extractor
```

### Neo4j 동기화
```bash
# 개별 동기화
python -m ingestion.graph_loader --sync-plans
python -m ingestion.graph_loader --sync-risk-events
python -m ingestion.graph_loader --sync-exclusions
python -m ingestion.graph_loader --sync-conditions

# 전체 동기화
python -m ingestion.graph_loader --all
```

### Dry-run (미리보기)
```bash
python -m ingestion.proposal_plan_extractor --dry-run
python -m ingestion.risk_event_extractor --dry-run
python -m ingestion.exclusion_extractor --dry-run
python -m ingestion.condition_extractor --dry-run
```

---

## 6. 현재 데이터 현황

| 테이블 | PostgreSQL | Neo4j | 설명 |
|--------|------------|-------|------|
| plan | 10건 | 10 nodes | 가입설계서별 1건 |
| plan_coverage | 1,086건 | 564 INCLUDES | Plan → Coverage 연결 |
| risk_event | 572건 | 572 nodes | 암, 뇌출혈 등 위험이벤트 |
| exclusion | 28건 | 28 nodes | 담보별 면책사유 |
| condition | 42건 | 42 nodes | 담보별 보장조건 (90일 대기 등) |

---

## 7. 향후 작업

| 작업 | 우선순위 | 설명 |
|------|----------|------|
| benefit_risk_event 연결 | 중 | Benefit ↔ RiskEvent N:M 연결 |
| Exclusion 추가 추출 | 낮 | clause_coverage 미연결 조항 처리 |
| 나이 조건 추출 | 낮 | min_age, max_age 패턴 개선 |

---

## 8. 문제 해결

### Q: Exclusion/Condition 추출이 적은 이유?
**A:** clause_coverage에 연결된 조항만 추출하기 때문.
- 전체 면책 조항: 3,796건
- clause_coverage 연결: 43건 → 28건 추출

### Q: RiskEvent가 Benefit과 연결 안 된 이유?
**A:** benefit_risk_event 테이블 연결 작업 미완료.
- 현재: RiskEvent만 독립 생성
- 향후: Benefit과 N:M 연결 필요

### Q: Plan과 Condition/Exclusion 관계는?
**A:** 직접 연결 없음. Coverage를 통해 간접 연결.
```
Plan → plan_coverage → Coverage → Condition/Exclusion
```
