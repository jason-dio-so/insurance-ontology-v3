# 🎨 프론트엔드 가이드

ChatGPT 스타일의 보험 온톨로지 AI 챗봇 프론트엔드 사용 가이드입니다.

> **최종 업데이트**: 2025-12-15

## 📋 목차

1. [시작하기](#시작하기)
2. [시스템 아키텍처](#시스템-아키텍처)
3. [UI 구성](#ui-구성)
4. [사용 방법](#사용-방법)
5. [쿼리 템플릿](#쿼리-템플릿)
6. [API 연동](#api-연동)
7. [컴포넌트 상세](#컴포넌트-상세)
8. [커스터마이징](#커스터마이징)

---

## 🚀 시작하기

### 방법 1: 스크립트 사용 (추천)

**터미널 1 - 백엔드 실행:**
```bash
./start_backend.sh
```

**터미널 2 - 프론트엔드 실행:**
```bash
./start_frontend.sh
```

### 방법 2: 수동 실행

**백엔드:**
```bash
uvicorn api.server:app --reload --host 0.0.0.0 --port 8000
```

**프론트엔드:**
```bash
cd frontend
npm install  # 처음 한 번만
npm run dev
```

### 접속

- **프론트엔드**: http://localhost:5173
- **백엔드 API**: http://localhost:8000
- **API 문서**: http://localhost:8000/docs

---

## 🏗️ 시스템 아키텍처

### 기술 스택

| 계층 | 기술 | 버전 |
|------|------|------|
| Frontend | React + TypeScript | React 18 |
| 빌드 도구 | Vite | 5.x |
| 스타일링 | Tailwind CSS | 3.x |
| HTTP 클라이언트 | Axios | - |
| 마크다운 | React Markdown | - |
| Backend | FastAPI (Python) | - |
| Database | PostgreSQL + pgvector | 16 |
| 그래프 DB | Neo4j | - |

### 디렉토리 구조

```
frontend/
├── src/
│   ├── components/           # React 컴포넌트
│   │   ├── Sidebar.tsx          # 좌측 네비게이션
│   │   ├── ChatInterface.tsx    # 메인 채팅 영역
│   │   ├── Message.tsx          # 개별 메시지 렌더링
│   │   ├── ComparisonTable.tsx  # 상품 비교 테이블
│   │   └── InfoQueryBuilder.tsx # 정보 조회 모달
│   ├── services/
│   │   └── api.ts               # Backend API 호출
│   ├── data/
│   │   └── queryTemplates.ts    # 쿼리 템플릿 정의
│   ├── types/
│   │   └── index.ts             # TypeScript 타입 정의
│   ├── App.tsx                  # 루트 컴포넌트 (상태 관리)
│   ├── main.tsx                 # 앱 엔트리 포인트
│   └── index.css                # 전역 스타일 (Tailwind)
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── tsconfig.json
```

### 컴포넌트 계층도

```
App (루트)
├── Sidebar (좌측)
│   ├── 처음으로 버튼
│   ├── 카테고리 목록
│   └── 템플릿 목록
└── ChatInterface (우측)
    ├── Header
    ├── Messages 영역
    │   └── Message (반복)
    │       ├── 마크다운 컨텐츠
    │       ├── ComparisonTable (선택적)
    │       └── Sources (선택적)
    ├── Input 영역
    └── InfoQueryBuilder 모달 (조건부)
```

### 데이터 현황 (DB)

| 항목 | 수량 | 설명 |
|------|------|------|
| 보험사 | 8개 | 삼성, 현대, DB, KB, 한화, 롯데, 메리츠, 흥국 |
| 상품 | 8개 | 각 보험사별 건강보험 1개씩 |
| 담보 | 294개 | 암, 뇌출혈, 수술비 등 |
| 급부 | 286개 | 담보별 보장금액 정보 |
| 문서 조항 | 80,602개 | 약관/사업방법서 청크 |
| 벡터 임베딩 | 67,841개 | 시맨틱 검색용 |

---

## 🖥️ UI 구성

### 왼쪽 사이드바 (Sidebar.tsx)

```
┌─────────────────────────┐
│  [🏠 처음으로] *조건부   │
├─────────────────────────┤
│  카테고리                │
│  ┌───────────────────┐  │
│  │ 상품 비교         │  │
│  │ 상품/담보 설명    │  │
│  │ 사용법            │  │
│  └───────────────────┘  │
├─────────────────────────┤
│  (카테고리 선택 후)      │
│  템플릿 목록             │
│  ▸ 다빈치로봇 암수술비   │
│  ▸ 뇌출혈 진단비 비교    │
│  ▸ 유사암 수술비 비교    │
│  ...                     │
├─────────────────────────┤
│  보험 온톨로지 AI v1.0   │
└─────────────────────────┘
```

**카테고리별 동작:**
| 카테고리 | 동작 |
|---------|------|
| 상품 비교 | 템플릿 선택 → 입력창에 예시 자동입력 → 수정/전송 |
| 상품/담보 설명 | 정보유형 선택 → InfoQueryBuilder 모달 열림 |
| 사용법 | 시스템 사용 안내 표시 |

### 오른쪽 채팅 인터페이스 (ChatInterface.tsx)

```
┌──────────────────────────────────────────────────┐
│  보험 온톨로지 AI 챗봇        [대화 초기화] *조건부│
├──────────────────────────────────────────────────┤
│                                                  │
│  (메시지 없음 시)                                 │
│  💬 환영 메시지 + 카테고리 안내                   │
│                                                  │
│  (메시지 있음 시)                                 │
│  👤 사용자 질문                                  │
│  🤖 AI 응답                                      │
│     ├─ 마크다운 텍스트 답변                      │
│     ├─ 📊 비교 테이블 (ComparisonTable)         │
│     └─ 📄 참고 자료 (Sources)                   │
│                                                  │
├──────────────────────────────────────────────────┤
│  [질문 입력창]                          [전송]   │
│  Enter로 전송 • Shift+Enter로 줄바꿈             │
└──────────────────────────────────────────────────┘
```

**입력창 상태:**
| 상태 | placeholder | 활성화 |
|------|------------|--------|
| 카테고리 미선택 | "왼쪽에서 카테고리를 선택해주세요" | ❌ |
| 상품/담보 설명 | "왼쪽에서 조회할 정보를 선택해주세요" | ❌ |
| 상품 비교 + 템플릿 미선택 | "왼쪽에서 비교할 담보를 선택해주세요" | ❌ |
| 상품 비교 + 템플릿 선택됨 | "질문을 입력하세요..." | ✅ |

---

## 💡 사용 방법

### 1. 상품 비교 흐름

```
1. 사이드바에서 "상품 비교" 카테고리 클릭
   └─ 비교 템플릿 목록 표시

2. 담보 템플릿 선택 (예: "다빈치로봇 암수술비 비교")
   └─ 입력창에 예시 질문 자동 입력
   └─ 입력창 활성화

3. 질문 수정 (선택사항) 후 Enter 또는 전송 버튼

4. 결과 확인
   └─ 마크다운 비교 분석
   └─ 비교 테이블 (회사/상품/담보/보장금액)
   └─ 참고 자료
```

### 2. 상품/담보 설명 조회

```
1. 사이드바에서 "상품/담보 설명" 카테고리 클릭

2. 정보 유형 선택 (예: "보장개시일", "면책사항")
   └─ InfoQueryBuilder 모달 열림

3. 단계별 선택
   Step 1: 보험사 선택 (8개사)
   Step 2: 상품 선택
   Step 3: 담보 선택 (보장금액 표시)
   (Step 4: 정보유형 - 이미 선택됨)

4. "조회하기" 클릭
   └─ 쿼리 자동 생성: "삼성 암진단비의 보장개시일은 언제인가요?"
   └─ API 호출 및 결과 표시
```

### 3. 비교 테이블 예시

**질문:**
```
삼성화재와 현대해상의 다빈치로봇 암수술비를 비교해주세요
```

**응답 구조:**
```
# 다빈치로봇 암수술비 비교

**삼성** (마이헬스파트너)
- 담보: 다빈치로봇 암수술비
- 보장금액: 3,000만원
- 가입나이: 15세~60세

**현대** (건강보험)
- 담보: 다빈치로봇 암수술비
- 보장금액: 2,500만원
- 가입나이: 15세~65세

## 주요 차이점
- 보장금액: 삼성이 현대보다 500만원 더 높습니다.

📊 비교 테이블
| 회사 | 상품 | 담보 | 보장금액 |
|------|------|------|----------|
| 삼성 | 마이헬스파트너 | 다빈치로봇 암수술비 | 3,000만원 |
| 현대 | 건강보험 | 다빈치로봇 암수술비 | 2,500만원 |

📄 참고 자료
[1] 삼성 - 마이헬스파트너: [119] 보장내용...
```

---

## 📝 쿼리 템플릿

> 템플릿 정의 파일: `frontend/src/data/queryTemplates.ts`

### 상품 비교 (5개)

| ID | 템플릿명 | 예시 질문 | 검색 키워드 |
|----|---------|-----------|------------|
| `compare-robot-surgery` | 다빈치로봇 암수술비 비교 | 삼성화재와 현대해상의 다빈치로봇 암수술비를 비교해주세요 | `다빈치` |
| `compare-brain-hemorrhage` | 뇌출혈 진단비 비교 | 삼성화재와 현대해상의 뇌출혈 진단비를 비교해주세요 | `뇌출혈` |
| `compare-quasi-cancer-surgery` | 유사암 수술비 비교 | 롯데손보와 한화손보의 유사암 수술비를 비교해주세요 | `유사암수술` |
| `compare-carcinoma-in-situ` | 제자리암 진단비 비교 | 삼성화재와 한화손보의 제자리암 진단비를 비교해주세요 | `제자리암` |
| `compare-borderline-tumor` | 경계성종양 진단비 비교 | 삼성화재와 한화손보의 경계성종양 진단비를 비교해주세요 | `경계성종양` |

### 상품/담보 설명 (5개)

| ID | 정보 유형 | 설명 | 검색 키워드 |
|----|----------|------|------------|
| `coverage-start-date` | 보장개시일 | 보장이 시작되는 시점과 면책기간 | 보장개시, 면책기간, 책임개시 |
| `coverage-limit` | 보장한도 | 보장금액의 한도와 지급 제한사항 | 보장한도, 지급한도, 지급제한 |
| `enrollment-age` | 가입나이 | 가입 가능한 나이 범위 | 가입, 나이 |
| `exclusions` | 면책사항 | 보장이 제외되는 경우 | 면책, 보장제외, 보상하지 |
| `renewal-info` | 갱신기간 및 비율 | 갱신 주기와 감액 비율 | 갱신, 감액 |

### 템플릿 구조

```typescript
interface QueryTemplate {
  id: string;              // 고유 ID
  category: string;        // 카테고리 (상품 비교, 상품/담보 설명, 사용법)
  title: string;           // 표시 제목
  description: string;     // 설명
  example: string;         // 예시 질문 (입력창에 자동 입력)
  tags: string[];          // 태그
  searchParams?: {         // 검색 파라미터 (상품 비교용)
    coverageKeyword?: string;   // 담보 키워드
    exactMatch?: boolean;       // 정확 매칭 여부
    excludeKeywords?: string[]; // 제외 키워드
    docTypes?: string[];        // 문서 타입 제한
  };
}
```

---

## 🔌 API 연동

> API 서비스 파일: `frontend/src/services/api.ts`

### 엔드포인트 목록

| 함수 | 메서드 | 엔드포인트 | 설명 |
|------|--------|-----------|------|
| `hybridSearch` | POST | `/api/hybrid-search` | 하이브리드 검색 (메인) |
| `healthCheck` | GET | `/health` | 서버 상태 확인 |
| `getCompanies` | GET | `/api/companies` | 보험사 목록 |
| `getCompanyProducts` | GET | `/api/companies/{company}/products` | 상품 목록 |
| `getProductCoverages` | GET | `/api/companies/{company}/products/{product}/coverages` | 담보 목록 |
| `getCompanyCoverages` | GET | `/api/companies/{company}/coverages` | 회사별 담보 목록 |

### 메인 검색 API 요청/응답

**요청 (HybridSearchRequest):**
```typescript
{
  query: string;                    // 사용자 질문
  userProfile?: UserProfile;        // 사용자 프로필 (선택)
  selectedCategories?: string[];    // 선택된 카테고리
  selectedCoverageTags?: string[];  // 선택된 담보 태그
  lastCoverage?: string;            // 이전 대화의 담보명 (컨텍스트)
  templateId?: string;              // 템플릿 ID
  searchParams?: SearchParams;      // 구조화된 검색 파라미터
}
```

**응답 (HybridSearchResponse):**
```typescript
{
  answer: string;                      // LLM 생성 답변 (마크다운)
  comparisonTable?: ComparisonResult[]; // 비교 테이블 데이터
  sources?: Source[];                  // 출처 정보
  coverage?: string;                   // 사용된 담보명 (컨텍스트용)
}
```

### API 처리 로직 분기

```
/api/hybrid-search 요청
         │
         ▼
┌─────────────────┐
│ NL Mapper로     │
│ 엔티티 추출     │
│ (회사명, 담보)  │
└─────────────────┘
         │
    ┌────┴────┬────────────┐
    ▼         ▼            ▼
┌────────┐ ┌────────┐ ┌────────┐
│회사 1개│ │회사 2개+│ │일반검색│
│+Info   │ │비교요청 │ │        │
└────────┘ └────────┘ └────────┘
    │         │            │
    ▼         ▼            ▼
InfoExtractor ProductComparer HybridRetriever
(정보추출)    (상품비교)     (벡터검색)
```

---

## 🧩 컴포넌트 상세

### App.tsx (루트 컴포넌트)

**역할**: 전역 상태 관리 및 자식 컴포넌트 조율

**상태 관리:**
| 상태 | 타입 | 설명 |
|------|------|------|
| `selectedTemplate` | `QueryTemplate \| null` | 현재 선택된 쿼리 템플릿 |
| `showInfoQueryBuilder` | `boolean` | 정보조회 모달 표시 여부 |
| `infoQueryTemplate` | `QueryTemplate \| null` | 정보조회용 템플릿 |
| `selectedCategory` | `string \| null` | 선택된 카테고리 |
| `resetTrigger` | `number` | 상태 초기화 트리거 |

**핸들러:**
- `handleSelectTemplate()`: 카테고리에 따라 템플릿/모달 처리
- `handleCategoryChange()`: 카테고리 변경 시 상태 초기화
- `handleNewChat()`: 전체 상태 초기화

### ChatInterface.tsx (메인 채팅)

**내부 상태:**
| 상태 | 설명 |
|------|------|
| `messages` | 대화 메시지 배열 |
| `input` | 현재 입력 텍스트 |
| `isLoading` | API 호출 중 여부 |
| `lastCoverage` | 이전 대화의 담보명 (컨텍스트) |
| `currentTemplate` | 현재 활성 템플릿 |
| `showReturnButton` | 처음으로 버튼 표시 여부 |

**메시지 전송 플로우:**
```
handleSubmit()
    │
    ├─ 1. 사용자 메시지 생성 & 추가
    ├─ 2. hybridSearch API 호출
    ├─ 3. 응답 메시지 생성 & 추가
    └─ 4. lastCoverage 업데이트 (컨텍스트)
```

### InfoQueryBuilder.tsx (정보조회 모달)

**단계별 흐름:**
```
Step 1: 보험사 선택
   └─ GET /api/companies
        │
        ▼
Step 2: 상품 선택
   └─ GET /api/companies/{company}/products
        │
        ▼
Step 3: 담보 선택
   └─ GET /api/companies/{company}/products/{product}/coverages
        │
        ▼
(Step 4: 정보유형 - preselected 시 스킵)
        │
        ▼
"조회하기" 클릭 → 쿼리 생성 → onQuerySubmit() 콜백
```

### 상태 초기화 규칙

| 트리거 | 초기화 대상 |
|--------|------------|
| "처음으로" 버튼 | 모든 상태 (App + ChatInterface) |
| 다른 카테고리 선택 | selectedTemplate, messages, input, resetTrigger++ |
| 대화 초기화 버튼 | messages, lastCoverage, input, currentTemplate |

---

## 🎨 커스터마이징

### 새로운 쿼리 템플릿 추가

`frontend/src/data/queryTemplates.ts` 파일 수정:

```typescript
{
  id: 'my-new-template',
  category: '상품 비교',
  title: '새로운 템플릿',
  description: '설명',
  example: '예시 질문',
  tags: ['태그1', '태그2'],
}
```

### 색상 변경

`frontend/tailwind.config.js` 파일의 `theme.extend.colors` 섹션 수정:

```javascript
colors: {
  'chat-bg': '#343541',        // 채팅 배경
  'sidebar-bg': '#202123',     // 사이드바 배경
  'user-msg': '#343541',       // 사용자 메시지 배경
  'assistant-msg': '#444654',  // AI 메시지 배경
}
```

### API 엔드포인트 변경

`frontend/src/services/api.ts` 파일의 `API_BASE_URL` 수정:

```typescript
const API_BASE_URL = 'http://localhost:8000';  // 여기 수정
```

---

## 🐛 문제 해결

### 프론트엔드가 시작되지 않을 때

```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### API 연결 오류

1. 백엔드가 실행 중인지 확인: http://localhost:8000/health
2. CORS 설정 확인: `api/server.py`의 `allow_origins`
3. 브라우저 콘솔에서 에러 메시지 확인

### 빌드 오류

```bash
cd frontend
npm run build -- --debug
```

---

## 📚 추가 리소스

- [React 문서](https://react.dev/)
- [Tailwind CSS 문서](https://tailwindcss.com/)
- [Vite 문서](https://vitejs.dev/)
- [FastAPI 문서](https://fastapi.tiangolo.com/)

---

## 🔍 알려진 이슈

### 1. 회사명 불일치
- **문제**: DB는 단축명(삼성, 현대, DB), Frontend 템플릿은 풀네임(삼성화재, 현대해상)
- **영향**: NL Mapper가 별칭 변환하지만 일부 API에서 불일치 가능
- **해결**: Backend NL Mapper의 `COMPANY_ALIASES` 활용

### 2. 제한된 템플릿
- **문제**: DB에 294개 담보가 있으나 Frontend에는 5개 비교 템플릿만 존재
- **해결 계획**: DB 기반 동적 템플릿 생성

### 3. 자유 질문 불가
- **문제**: 카테고리 선택 없이는 입력창 비활성화
- **해결 계획**: "자유 질문" 모드 추가

### 4. LLM 응답 비활성화
- **문제**: 일반 검색에서 LLM 응답 비활성화 (server.py:721-722)
- **영향**: 검색 결과만 표시, 자연어 답변 없음
- **상태**: 검토 후 활성화 예정

---

## 🎯 개발 로드맵

### 완료
- [x] 기본 UI (사이드바 + 채팅)
- [x] 상품 비교 기능
- [x] 상품/담보 설명 조회 (InfoQueryBuilder)
- [x] 비교 테이블 렌더링
- [x] 마크다운 응답 렌더링

### 진행 예정
| 우선순위 | 항목 | 설명 |
|---------|------|------|
| 1 | 회사명 동기화 | DB-Frontend 간 회사명 매핑 통일 |
| 2 | 자유 질문 모드 | 카테고리 없이 직접 질문 가능 |
| 3 | 템플릿 동적 생성 | DB 담보 기반 템플릿 자동 생성 |
| 4 | 전체 보험사 비교 | InfoQueryBuilder에서 전체 비교 옵션 |
| 5 | UX 개선 | 에러 처리, 로딩 상태 개선 |

### 향후 계획
- [ ] 사용자 프로필 저장
- [ ] 대화 히스토리 저장
- [ ] PDF 다운로드 기능
- [ ] 다크/라이트 모드 토글

---

## 📚 관련 문서

- [frontend/frontend_spec.md](frontend/frontend_spec.md) - API 명세서
- [ontology/README.md](ontology/README.md) - 온톨로지 모듈 설명
- [docs/](docs/) - 추가 문서

---

*최종 업데이트: 2025-12-15*

**문의사항이 있으시면 이슈를 등록해주세요!** 🙌
